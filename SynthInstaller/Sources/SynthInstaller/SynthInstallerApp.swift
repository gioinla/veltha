import SwiftUI

@main
struct SynthInstallerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 750, height: 700)
    }
}
